{
  "scripts": {
    "start": "run-p dev watch",
    "build" : "sass sass:css",
    "watch" : "npm run build -- --watch",
    "dev" : "live-server"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.43.4"
  }
}
